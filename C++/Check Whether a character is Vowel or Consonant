#include<iostream>
using namespace std;
int main(){
  char a='A';
  if(a=='a' || a=='e' || a=='i' || a=='o' || a=='u'){
    cout<<"Vowel";
  }
  else if (a=='A' || a=='E' || a=='I'|| a=='O' || a=='U')
  {
    cout<<"Vowel";
  }
  
  else{
    cout<<"consonent";
  }
    return 0;
}
